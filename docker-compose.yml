
version: '3.7'
services:
  app:
    container_name: student_grouper_container
    build: .
    stdin_open: true
    tty: true
    ports:
      - '8000:8000'
    hostname: 0.0.0.0
    volumes:
      - ./app:/app
